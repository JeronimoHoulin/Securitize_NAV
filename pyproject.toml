[project]
name = "securitize-nav"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.9",
    "openpyxl>=3.1.5",
    "pandas>=3.0.2",
    "python-dotenv",
    "web3",
]

[dependency-groups]
dev = [
    "pip-audit>=2.10.0",
]
